- if (local_assigns[:include_link] && keyword.status == 'success') || false
  %td= link_to(keyword.name, keyword_path(keyword))
- else
  %td= keyword.name

%td= keyword.status.humanize
%td= format_date(keyword.created_at)
