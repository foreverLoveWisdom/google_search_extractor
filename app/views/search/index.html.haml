%h1 Search Across All Reports
= form_with(url: search_path, method: :get) do |f|
  .mb-3
    = f.text_field :keyword, class: 'form-control', placeholder: 'Search keyword'
  .mb-3
    = f.submit 'Search', class: 'btn btn-primary'

- if @keywords.present?
  = render 'search_across_report', keywords: @keywords
- else
  %p No matching keywords found.
